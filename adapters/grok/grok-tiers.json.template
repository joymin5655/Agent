{
  "comment": "Owned by the xAI adapter — model IDs are forbidden in core/infra/backends.json (no-model-ids gate), so the pin lives here. Install: cp grok-tiers.json.template ~/.grok/agent-tiers.json (setup.sh does this). grok CLI 0.2.x exposes a single model; tiers differ by reasoning effort only.",
  "model": "grok-4.6",
  "tiers": {
    "MID": [],
    "TOP": ["--reasoning-effort", "high"]
  }
}
