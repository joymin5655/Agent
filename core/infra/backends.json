{
  "version": 1,
  "comment": "Machine-readable SSOT for cross-vendor worker dispatch (docs/model-routing.md § Cross-vendor second-opinion lane). Roles map to backends, backends map to CLI argv. Model names NEVER appear here — each vendor's adapter/profile owns its tier (codex --profile deep = TOP-tier reasoning per the enforcement map).",
  "roles": {
    "second-opinion-review": { "backend": "codex", "fallback": "gemini" },
    "second-opinion-verify": { "backend": "codex", "fallback": null }
  },
  "backends": {
    "codex": {
      "connection": "cli",
      "cmd": ["codex", "exec", "--profile", "deep"],
      "timeout_s": 300
    },
    "gemini": {
      "connection": "cli",
      "cmd": ["gemini", "-p"],
      "timeout_s": 300
    }
  }
}
